# マルチLLM リフレクションパターン 依存関係

# NOTE: 軽量エンジンへ移行したため agent-framework 依存は削除。
# DevUIは現状サポート対象外。

# 型検証とJSONパース用
pydantic>=2.12.5

# 環境変数の読み込み
python-dotenv>=1.2.1

# 設定ファイル解析用
pyyaml>=6.0.3

# LLM SDKs
openai>=2.15.0
anthropic>=0.76.0
google-genai>=1.60.0
