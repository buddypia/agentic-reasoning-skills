# マルチLLM リフレクションパターン 依存関係

# NOTE: 軽量エンジンへ移行したため agent-framework 依存は削除。
# DevUIは現状サポート対象外。

# 型検証とJSONパース用
pydantic>=2.12.5

# 環境変数の読み込み
python-dotenv>=1.2.1

# 設定ファイル解析用
pyyaml>=6.0.3

# 백엔드 = 구독 인증 CLI (claude / codex / agy) — provider REST SDK 불필요.
# CLI 호출은 표준 라이브러리 subprocess 만 사용.
